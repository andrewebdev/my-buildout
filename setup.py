from setuptools import setup, find_packages

setup(
    name="PROJECT_NAME",
    version="0.1",
    url="",
    description="",
    author="Your Name",
    packages=find_packages('src'),
    package_dir={'': 'src'},
    install_requires=[
        'setuptools',
    ],
)
